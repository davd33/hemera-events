# hemera-events
Events shared to the system.

## Install fuge:
`npm i -g fuge`

## Run Shell
`npm start`

## Start Event Servers
`start onEventImpl1`
Then:
`start onEventImpl2`

## Publish an event
`start publishEvent`
